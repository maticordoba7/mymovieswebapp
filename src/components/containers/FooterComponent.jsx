import { Layout } from 'antd';
import React from 'react';

function FooterComponent(props) {
  const { Footer } = Layout;

  return (
    <Footer style={{ textAlign: 'center' }}>Created by Córdoba, Matías</Footer>
  );
}

export default FooterComponent;
